import { FirstnameDirective } from './firstname.directive';

describe('FirstnameDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstnameDirective();
    expect(directive).toBeTruthy();
  });
});
